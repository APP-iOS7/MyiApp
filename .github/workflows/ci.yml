name: CI

on:
  pull_request:
    branches:
      - develop
      - main
  push:
    branches:
      - develop
      - main

permissions:
  id-token: write
  contents: read

jobs:
  format:
    uses: ./.github/workflows/format.yml

  test:
    needs: format
    uses: ./.github/workflows/test.yml

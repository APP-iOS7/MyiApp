name: Format

on:
  workflow_call:

jobs:
  format:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: jdx/mise-action@v4

      - name: Swift format
        run: |
          swiftformat .
          git diff --stat
          git diff
          git diff --exit-code

name: Test

on:
  workflow_call:

permissions:
  id-token: write
  contents: read

jobs:
  test:
    runs-on: macos-latest
    env:
      TUIST_XDG_STATE_HOME: /tmp/tuist-state
    steps:
      - uses: actions/checkout@v4

      - uses: jdx/mise-action@v2

      - name: Tuist auth login
        run: tuist auth login

      - name: Tuist setup cache
        run: tuist setup cache

      - name: Clean selective tests cache
        run: tuist clean selectiveTests

      - name: Run tests
        run: tuist test MyiApp

      - name: Upload Tuist session logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: tuist-session-logs
          path: /tmp/tuist-state/tuist/sessions/
          if-no-files-found: ignore

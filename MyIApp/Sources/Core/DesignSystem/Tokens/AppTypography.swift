import Foundation

extension DesignSystem {
    /// 타이포그래피에 대한 토큰 정의입니다.
    public enum Typography {
        /// 컴포넌트 내부 텍스트 크기 (기본 18.5)
        public static let componentFontSize: CGFloat = 18.5
        /// 헤더/로고 텍스트 크기 (기본 60)
        public static let headerLogoSize: CGFloat = 60
        /// 아이콘 크기 (기본 16)
        public static let iconSize: CGFloat = 16
    }
}

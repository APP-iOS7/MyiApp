import Foundation

extension DesignSystem {
    /// 곡률(Corner Radius)에 대한 토큰 정의입니다.
    public enum Radius {
        /// 기본 곡률 (기본 5)
        public static let medium: CGFloat = 5
        /// 카드/컨테이너용 곡률 (기본 12)
        public static let card: CGFloat = 12
    }
}

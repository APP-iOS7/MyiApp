import Foundation

extension DesignSystem {
    /// 컴포넌트 및 이미지 크기에 대한 토큰 정의입니다.
    public enum Sizing {
        /// 메인 일러스트레이션 크기 (기본 431)
        public static let illustrationLarge: CGFloat = 431
    }
}
